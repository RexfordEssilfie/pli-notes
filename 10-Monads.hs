module Monads where

-- TODO: write me!

-- class Applicative m => Monad m where
--   return :: a -> m a
--   return = pure

--   (>>=) :: m a -> (a -> m b) -> m b

-- instance Monad Maybe where
--   Nothing >>= _ = Nothing
--   Just x >>= f = f x